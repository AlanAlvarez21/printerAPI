CREATE DATABASE examen;

CREATE TABLE users (
    id VARCHAR(255) PRIMARY KEY,
    name VARCHAR(30),
    age INT,
    gender VARCHAR(255),
    email VARCHAR(255) PRIMARY KEY,
);


-- INSERT INTO users(id, age, gender, email) VALUES('0', 27, 'male','alanbrito@yopmail.com');
